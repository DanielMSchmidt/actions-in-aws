# Example environment variables for the TODO List Lambda project.
# Copy this file to `.env` (DO NOT COMMIT your real credentials) and adjust values as needed.

# PostgreSQL connection string:
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
# For Neon / Supabase / RDS etc. include any required SSL/query params.
DATABASE_URL=postgres://user:password@localhost:5432/todos_dev

# Notes:
# - This variable MUST be set in the Lambda function configuration (or via your IaC tool).
# - Keep the pool size low in serverless environments to avoid exhausting DB connections.
# - If using a cloud Postgres that requires SSL, append ?sslmode=require (or provider-specific params).
#
# Examples:
#   postgres://app:secret@db.internal:5432/todos
#   postgres://user:pass@host:5432/todos?sslmode=require
#
# After setting DATABASE_URL locally you can run:
#   npm install
#   npm run generate   # generate migrations from schema
#   npm run migrate    # apply migrations
#   npm run dev        # start local watcher (for invoking the handler manually)